/** Chrome storage keys for note defaults (popup + background must stay in sync). */
export const NOTE_EXPIRES_AFTER_MINUTES_KEY = "noteExpiresAfterMinutes";
export const NOTE_MAX_VIEWS_KEY = "noteMaxViews";

/** First-run default: 1 day, aligned with popup hint. */
export const DEFAULT_NOTE_EXPIRES_AFTER_MINUTES = 1440;
export const DEFAULT_NOTE_MAX_VIEWS = 3;
